import { TrackStatusPipe } from './track-status-pipe';

describe('TrackStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new TrackStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
